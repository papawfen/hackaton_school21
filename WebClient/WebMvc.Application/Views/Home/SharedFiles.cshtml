@page
@using WebMvc.Application.Models

@{
    ViewBag.Title = "SharedFiles";
}

@{
    await Html.RenderPartialAsync("_FilesList", new List<MediaEntry>
    {
        new MediaEntryBuilder().SetPreviewIconPath("totally/not/fake/icon/path").SetName("We are not gonna implement this feature right now!").SetSize(777).Build()
    });
}